# Database Folder

This folder is used to store the SQLite database generated from cleaned datasets.

The database file is not uploaded to GitHub because it can be regenerated by running:

```bash
python -m src.database_loader
```

This keeps the repository lightweight and reproducible.