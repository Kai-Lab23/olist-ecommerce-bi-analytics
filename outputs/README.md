# Outputs Folder

This folder stores generated outputs such as:

* Data quality reports
* SQL query results
* Power BI-ready datasets

Generated output files are not uploaded to GitHub because they can be reproduced by running the project scripts.

To regenerate outputs, run the relevant Python scripts from the project root folder.