# Dashboard Folder

This folder is used to store the Power BI dashboard file.

The `.pbix` file is not uploaded to GitHub because it may be large and is excluded using `.gitignore`.

Dashboard screenshots are available in the `reports/` folder.